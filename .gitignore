*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
/.credential/PAT.txt
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db

# curl test artifacts
cookie.txt

# Graphify build artifacts & caches (regenerate with `graphify extract`)
/graphify-out

# Git credentials (do not commit)
.git-credentials

# Brand asset workspace (dev scratch, not shipped)
/logo_work/

# Local tooling & audit artifacts (not shipped)
/.claude/
/composer.phar
/composer-update.phar
/lighthouse-report.json
/public_static/

# Credentials (never commit — repo is public)
/.credential/
/.credentian/
